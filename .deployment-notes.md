# Deployment Notes - Agent Service Setup

## What Was Fixed

1. **RBAC** - Azure AI User role at PROJECT scope (not account scope)
2. **Agent Creation** - Fixed REST API endpoint to `/assistants`
3. **Agent ID** - Proper format starting with `asst_`
4. **Model** - Changed to gpt-4o (more stable)
5. **Message Retrieval** - Filter for assistant messages

## Files Modified

- `infrastructure/modules/rbac.bicep` - Added Azure AI User at project scope
- `infrastructure/main.bicep` - Pass project name to rbac module
- `create-persistent-agent.sh` - Fixed endpoint, updated to gpt-4o
- `backend/src/utils/agentService.ts` - Fixed message retrieval
- `backend/src/functions/generateQuestions.ts` - Improved JSON parsing

## Deployment

```bash
./deploy-full-development.sh
```

Automatically handles:
- Infrastructure with correct RBAC
- Agent creation/reuse
- Function App configuration
- Backend and frontend deployment

## Documentation

See `AGENT_SERVICE_GUIDE.md` for complete details.
